Aqui está a sua lista com **comentários usando `//` em uma única linha explicando cada método/propriedade**:

---

### 🔎 **Selecionar elementos**

* `document.getElementById(id)` // Seleciona um elemento pelo id
* `document.getElementsByClassName(classe)` // Seleciona todos os elementos com uma classe
* `document.getElementsByTagName(tag)` // Seleciona todos os elementos por nome da tag
* `document.querySelector(seletor)` // Seleciona o primeiro elemento que corresponde ao seletor CSS
* `document.querySelectorAll(seletor)` // Seleciona todos os elementos que correspondem ao seletor CSS

---

### ➕ **Criar elementos**

* `document.createElement(tag)` // Cria um novo elemento HTML
* `document.createTextNode(texto)` // Cria um nó de texto

---

### 📌 **Manipular conteúdo**

* `element.innerHTML` // Define ou retorna o HTML interno do elemento
* `element.innerText` // Define ou retorna o texto visível do elemento
* `element.textContent` // Define ou retorna todo o texto do elemento (visível ou não)
* `element.outerHTML` // Retorna ou altera o próprio elemento completo

---

### 🧩 **Manipular atributos**

* `element.getAttribute(atributo)` // Obtém o valor de um atributo
* `element.setAttribute(atributo, valor)` // Define ou altera um atributo
* `element.removeAttribute(atributo)` // Remove um atributo
* `element.hasAttribute(atributo)` // Verifica se o atributo existe

---

### 🎨 **Manipular classes**

* `element.classList.add(classe)` // Adiciona uma classe ao elemento
* `element.classList.remove(classe)` // Remove uma classe do elemento
* `element.classList.toggle(classe)` // Alterna uma classe (adiciona/remove)
* `element.classList.contains(classe)` // Verifica se o elemento tem determinada classe
* `element.className` // Obtém ou define todas as classes do elemento

---

### 📏 **Manipular estilos**

* `element.style.propriedade` // Define estilos CSS diretamente no elemento
* `getComputedStyle(element)` // Obtém os estilos finais calculados do elemento

---

### 🧱 **Manipular a árvore DOM**

* `element.appendChild(filho)` // Adiciona um filho no final do elemento
* `element.prepend(filho)` // Adiciona um filho no início do elemento
* `element.remove()` // Remove o próprio elemento do DOM
* `element.removeChild(filho)` // Remove um filho específico
* `element.replaceChild(novo, antigo)` // Substitui um filho por outro
* `element.cloneNode(true|false)` // Clona o elemento (true clona filhos, false não)
* `element.insertBefore(novo, referencia)` // Insere um elemento antes de outro

---

### 🎧 **Eventos**

* `element.addEventListener(evento, funcao)` // Adiciona um ouvinte de evento ao elemento
* `element.removeEventListener(evento, funcao)` // Remove um ouvinte de evento

---

### 🧭 **Navegação entre elementos**

* `element.parentElement` // Retorna o elemento pai
* `element.children` // Retorna os filhos diretos do elemento
* `element.firstElementChild` // Retorna o primeiro filho
* `element.lastElementChild` // Retorna o último filho
* `element.nextElementSibling` // Retorna o próximo irmão
* `element.previousElementSibling` // Retorna o irmão anterior

---

### 📝 **Formulários**

* `input.value` // Obtém ou define o valor de um campo
* `input.checked` // Verifica se o checkbox/radio está marcado
* `form.submit()` // Envia o formulário via JavaScript
* `form.reset()` // Reseta os campos do formulário

---

### ⏱️ **Timers**

* `setTimeout(funcao, tempo)` // Executa uma função após um tempo
* `setInterval(funcao, tempo)` // Executa uma função repetidamente em intervalos
* `clearTimeout(id)` // Cancela um setTimeout
* `clearInterval(id)` // Cancela um setInterval

---

### 🖥️ **Window / Document**

* `window.location` // Obtém ou altera a URL atual
* `window.history` // Acessa o histórico do navegador
* `window.scrollTo(x, y)` // Faz a página rolar até uma posição específica
* `window.alert()` // Exibe um alerta na tela
* `window.confirm()` // Exibe uma caixa de confirmação (OK/Cancelar)
* `window.prompt()` // Exibe uma caixa para inserir texto

---

### 🧹 **Outros úteis**

* `element.focus()` // Foca o elemento
* `element.blur()` // Remove o foco do elemento
* `element.scrollIntoView()` // Rola a página até o elemento

---

Se quiser, posso te entregar isso em **formato de arquivo .txt ou .md** pra você usar como cola de estudos.
